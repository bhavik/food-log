import { getDefaultCategories } from './dietPlans';

/** Default meal categories (Gujarati Low Sugar Low Fat). Use diet plans for onboarding. */
export const MEAL_CATEGORIES = getDefaultCategories();

export const STORAGE_KEY = 'foodlog_v1_data';
